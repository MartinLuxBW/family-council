# Optional: enables real AI cartoon avatar generation (from photo or description).
# Without a key, the app falls back to a built-in cartoon avatar generator.
# Copy this file to .env.local and fill in your key.
OPENAI_API_KEY=
